# Matrizes

  Uma Matriz é um vetor (array) multidimensional, geralmente empregado no armazenamento de dados em forma de tabela. A representação mais simples de uma Matriz é por um Array bidimensional, isto é, uma estrutura contendo ```n```
linhas por ```m``` colunas como mostra a figura abaixo:

 ![img_matriz]()

   Da mesma maneira do que nos Vetores, os índices nas matrizes começam no inteiro ```0```  
